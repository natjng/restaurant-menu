<br>
<div>
    <%= link_to @item.menu.restaurant.name, @item.menu.restaurant %> / <%= link_to @item.menu.name, @item.menu %> / <%= link_to @item.category.name, @item.category %>
</div>

<br>
<br>

<div class="w-50 p-3">
    <img src="<%= @item.image %>" class="img-fluid">
</div>

<h1><%= @item.name %></h1><br />

<div>
    <%= number_to_currency(@item.price) %><br />
    <%= @item.description %><br />
</div>

<br />
<br />

<%= link_to "Edit Item", edit_item_path(@item) %> | <%= link_to "Delete Item", @item, method: :delete, data: { confirm: "Are you sure?" } %>