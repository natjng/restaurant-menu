<h1>Menus</h1>

<ul>
<% current_user.restaurants.each do |r| %>
    <li>
        Restaurant: <%= link_to r.name, r %>
        Menus: 
        <% r.menus.each do |menu| %> | 
            <%= link_to menu.name, menu %>
        <% end %>
    </li>
<% end %>
</ul>